import { invalidStorybookBaseDirectory } from './invalidStorybookBaseDirectory';

export default {
  title: 'CLI/Messages/Errors',
};

export const InvalidStorybookBaseDirectory = () => invalidStorybookBaseDirectory();
