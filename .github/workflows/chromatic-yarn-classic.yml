name: "Chromatic (yarn 1.x.x)"
on: [push]
# do not use pull_request here, it report the wrong commit_sha and commit_ref

jobs:
  chromatic:
    runs-on: ubuntu-latest
    steps:
    - uses: actions/setup-node@v1
      with:
        node-version: '14.x'
    - uses: actions/checkout@v1
    - name: switch to yarn classic
      run: yarn set version 1.x.x
    - name: install
      run: yarn
    - name: run chromatic
      run: npx -p . chromatic
      env: 
        CHROMATIC_PROJECT_TOKEN: gcaw1ai2dgo
